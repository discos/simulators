sudo: false
language: python

matrix:
  include:
    - python: 2.7
      env:
          TOXENV=py,codecov,coverage-report,doctest,doc-html,doc-linkcheck,doc-doctest

install:
  - pip install tox

script:
  - tox

cache:
  - pip

notifications:
  email: true
